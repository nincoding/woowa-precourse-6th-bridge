const GAME_CONDITION = Object.freeze({
  bridgeMinRange: 3,
  bridgeMaxRange: 20,
  movingUp: 'U',
  movingDown: 'D',
  retryCommand: 'R',
  endCommand: 'Q',
});

const SYMBOL = Object.freeze({
  emptyString: '',
});

export { GAME_CONDITION, SYMBOL };
